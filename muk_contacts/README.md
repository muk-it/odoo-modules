# MuK Contacts

This module improves and extends the contact app and the related partner model.
In addition to an improved list view with additional fields with custom widgets,
it adds a contact number field.
