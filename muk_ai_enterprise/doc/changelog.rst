`1.1.0`
-------

- A chat started from a record — a mention in a chatter, a run fired by an
  automation — now carries the Enterprise context of that record, the same
  one the chat window gathers when the record is on screen
- The writing helper takes the place of Enterprise's own button in the mail
  composer, so there is one way to write there rather than two

`1.0.0`
-------

- Initial Release
