# MuK List Columns

When manually changing the column width, the changed width is saved in local
storage. When loading the view, the saved values are used to define the column
width. Double-clicking on the column resize allows the original width to be
restored. This also deletes the saved value.
