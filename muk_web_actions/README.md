# MuK Web Actions

Technical module to provide the possibility to execute server actions and reports
in batches on a client side. This makes it possible not to reach the server
timeout limit even with large numbers of records.
